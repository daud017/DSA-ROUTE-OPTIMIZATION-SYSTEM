# DSA-ROUTE-OPTIMIZATION-SYSTEM
This project builds a Route Optimization System that solves TSP using three different approaches. We represent the city map as a graph where cities are nodes and the distances between them are weighted edges. The project is built in C++ and has a console-based interface with ASCII visualization.
